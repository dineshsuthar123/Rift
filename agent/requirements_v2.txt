# RIFT v2 — Python Agent Dependencies (requirements_v2.txt)
# Install with: pip install -r requirements_v2.txt
# Pinned to ranges for security surface minimization.

# Supabase client
supabase>=2.10.0,<3.0.0

# HTTP client (replaces requests — smaller, async-capable)
httpx>=0.27.0,<1.0.0

# Anthropic SDK (Claude)
anthropic>=0.30.0,<1.0.0

# Static analysis
ruff>=0.8.0
mypy>=1.11.0

# AST parsing (optional Gate 2)
libcst>=1.4.0

# Testing + coverage
pytest>=8.3.0
pytest-cov>=5.0.0
pytest-timeout>=2.3.0
diff-cover>=9.2.0

# Mutation testing (optional Gate 4)
mutmut>=3.0.0

# Config parsing
PyYAML>=6.0.2

# Utilities
python-dateutil>=2.9.0
